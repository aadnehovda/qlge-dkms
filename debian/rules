#!/usr/bin/make -f

include /usr/share/dpkg/pkg-info.mk

%:
	dh $@ --with dkms

override_dh_install:
	dh_install README.md TODO LICENSE Makefile dkms.conf usr/src/qlge-$(DEB_VERSION_UPSTREAM)/
	dh_install src/* usr/src/qlge-$(DEB_VERSION_UPSTREAM)/src/

override_dh_auto_configure:

override_dh_auto_build:

override_dh_auto_test:

override_dh_auto_install:

override_dh_auto_clean:
